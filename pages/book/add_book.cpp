#include "add_book.h"

#include <iostream>
using namespace std;

void addBook(sqlite3* db) {
   // 在这里写logic
   cout << "\nPress Enter to return to menu...";
   cin.ignore();
   cin.get();  // Wait for user input before returning
}
